# Work lanes

Claims are effective only after this file is merged to the canonical remote branch. A
claim covers overlapping files and package surfaces, not merely a task title. If a
claim push races, the losing writer must reread this registry and abandon an occupied
lane rather than rebasing a duplicate claim. Existing claims require explicit takeover.

## ACTIVE

| lane | owner/session/worktree | base | scope and status |
|---|---|---|---|
| source-declared non-UIC transient execution | `/root/zenodo_access_diagnosis` (Codex Sol), `/mnt/volume1/code/wt/bw-circuit-ui-nonuic-transient-sol`, branch `lane/nonuic-source-analysis-sol` | `0026d780079c7b5112629e56c7fdcc54e0ed98dd` | **CLAIM 2026-09-14.** Consume the separately claimed engine initializer and run eligible static-source R/C/L `.tran` analyses through the public Circuit/source-analysis API. One-argument LTspice `.tran Tstop` uses an explicit bounded recorded observation-grid profile; ordinary `.tran Tstep Tstop` preserves its authored grid. Non-UIC initialization is explicit, `UIC` stays distinct, and `startup`/IC/nodeset/waveform/model/import blockers refuse. Add self-authored and fixed-private-corpus native/ngspice numerical comparisons. No ASC/ASY reader/exporter/document blocks, MOS/BJT equations, startup ramp substitution, or silent directive stripping. |
| broad LTspice ASC/ASY document interchange | `/root/asc_importer_rebuild` (Codex Sol), `/mnt/volume1/code/wt/cui-ltspice-asc-document-sol`, branch `lane/ltspice-asc-document-sol` | `797a77f` | **CLAIM 2026-09-14.** Replace the narrow native-only ASC gate with a bounded source-document reader that retains Version 4/4.1 records, unknown records, directives, instances, custom ASY pins/defaults and connectivity independently of bw-board simulation support; then project only semantically supported devices through existing native/SPICE contracts with explicit per-instance refusals. Scope: `src/importers/ltspice-asc.js`, `src/importers/ltspice-asy.js`, importer registry/options and import UI/CLI reachability where required, source-document persistence/model plumbing, `src/model/exporters/ltspice-asc.js` and exporter registry, focused ASC/ASY/interchange tests, self-authored fixtures, attribution/docs, and a new bounded corpus measurement script/result metadata (not corpus payloads). Round-trip acceptance includes structural terminal graphs/values/models for ASC↔SPICE and ASC↔KiCad wherever current converters can represent them, checked with an independent reader/tool so a shared parser defect cannot pass. No bw-board solver equations, generic SPICE parser semantics, palette/part sidecars, vendor symbol bundle, automatic file/network include resolution, or Brickwright Lite copy is in scope. Existing strict numerical-eligibility blockers remain; document usefulness is measured separately from solver completeness. |
| controlled sources as GUI parts (`vcvs`/`vccs`) | Claude Fable (simclaude2), `/mnt/volume1/code/wt/cui-vcvs-gui`, branch `lane/vcvs-vccs-gui-parts` | `aab38c0`, forward-merged through `b8e958e` | **DONE 2026-09-14**, after remotely merged claim `264865e`, which reversed the deliberate "importer-only, no UI sidecar/palette entry" ruling recorded in the DONE row *controlled-source slug-coverage CI repair* on the owner's instruction. `vcvs` and `vccs` now have sidecars, art, palette entries, a canvas face, canvas terminal geometry, schematic symbols, wire-router and hit bounds and BOM labels. Canvas geometry is DERIVED from the sidecar through one new helper, `sidecarCenterOffsets`, not retyped in the renderer. Palette entries state no `gain` and no `gm`, so an untouched part solves at bw-board's own defaults; the test reads those defaults out of `mna.js` rather than restating them. Unchanged, deliberately: `src/importers/spice.js` and `src/model/exporters/spice.js` (another lane's, and correct as landed, including G's `outn`/`outp` first-to-second mapping), the engine, and `F`/`H`, which stay refused by name because bw-board E3.5b is deferred by ruling. Exact-head CI run `34838575485` on `f6d4ea8` passed test, interaction-gate, SPICE-oracle and KiCad-oracle. The browser gate is the evidence that matters: two new scenarios assert both parts place from the palette, draw a body, and offer FOUR separately wireable terminals, and the CI log names both — 36 scenarios, 36 passed, roll-call 36/36. Locally 2,366 node tests pass, 0 fail, 18 skip; production build green. Eight mutations fire across the new gates. THREE DEFECTS FOUND IN THIS LANE'S OWN WORK, each worth more than the feature. (1) Sidecar terminal ORDER is the part's terminal order, and was unmarked: authoring these two in reading order broke the landed `outp/outn/inp/inn` contract through `Circuit.fromJSON`, while this lane's own test compared the same two things as SETS and stayed green. The schema now says so. (2) A clause asserting the palette restates no electrical default PASSED on a tree where the `opamp` offender was in plain sight, because the entry regex never matched a single-line entry; the extractor now proves it found 80+ entries before its output is believed, and the one real offender is a shrink-only ledger entry owned by the parts-library lane. (3) `scripts/verify-interaction.mjs` had been driving ANOTHER WORKING TREE since 2026-08-30: port 3142 is held by an orphaned vite (pid 1166699, PPID 1) from `wt/fab-cui`, and `--strictPort` does not protect because that server binds `[::1]` only while ours takes `127.0.0.1` and `localhost` resolves to the older one. Every local `verify:interaction` run in between was green about code not under test. The gate now probes BOTH stacks by address before spawning and refuses with exit 1; proven by running it against the occupied port. The port is named in exactly one file, enumerated rather than assumed (the other `3142` hits in the tree are coincidental digits inside PCB fixtures). The orphan was left alone. ALSO MEASURED, NOT FIXED: 33 of 95 palette kinds have no free-placement canvas geometry — 31 rescued by seating, 2 (`seven_seg_3`, `seven_seg_4`) broken everywhere. Both are shrink-only ledgers in `test/canvas-terminal-geometry.test.js`, with a separate cap so the broken pair cannot hide inside the rescued 31. 30 of the 31 have usable sidecar coordinates, so closing them is mechanical, but 27 have no canvas body at all and the seven-segment pair shows what a sidecar/face scale mismatch does — a follow-up lane with a visual check per part, not a blind sweep. Also deletes 23 dead `slug-coverage` EXCEPTIONS whose comments claimed kinds had no sidecar long after the art landed. |
| strict top-level constant parameter arithmetic | `/root/zenodo_access_diagnosis` (Codex Sol), `/mnt/volume1/code/wt/cui-constant-param-sol` | **DONE 2026-09-14 in `d29e769`.** | A dependency-free bounded evaluator resolves top-level SPICE `.param` definitions and ASC-authored `TEXT !.param` values for supported scalar R/C/L/V/I/E/G fields. Independent V/I AC magnitude/phase metadata remains separate from explicit or zero DC bias, and unresolved expressions persist as structured per-part/Circuit blockers. Grammar is finite SPICE literals, case-folded names, parentheses, unary signs and `+ - * /`; functions, behavioral/time expressions, subcircuit-local hoisting, model conversion and external paired-definition enrichment remain explicit refusals. Focused integrated gate: 75/75; full local gate: 2,301 passed, 18 skipped, 0 failed; production build green. Fixed private 16-row upper bound: 15 import-lossless, with the function/behavioral row correctly refused. Exact-head CI run `34806859554` passed test, interaction, SPICE-oracle and KiCad-oracle jobs before guarded fast-forward. |
| LTspice ASC instance-record and mirror orientations | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13**, after remotely merged claim `a56eaeb`. `WINDOW` display records no longer terminate their owning symbol before later `SYMATTR` records. Added the format's `M0/M90/M180/M270` matrices only for the already-supported standard R/C/static V/I symbols. Unknown symbols, semantic attributes, dynamic values and unsupported directives remain explicit refusals/losses; no solver, GUI or other importer changed. | Standard-symbol matrix checks plus asymmetric mirrored voltage/resistor/capacitor and signed current/resistor fixtures pass; the M270 current result independently agrees with ngspice 42. Focused importer/reachability/registration gate passes 35/35 and production build passes. The fixed 8,280-row private no-aug scan contains 0 `WINDOW` records; mirrors remove a geometry blocker from 1,265 rows but produce 0 new lossless rows. The sole apparent candidate exposes its missing/non-static Value and Value2 losses once mapped, so it remains correctly refused rather than becoming a false pass. |
| bounded LTspice ASC static importer | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13.** Added a content-detected `.asc` importer and menu registration for the verified standard resistor/static-voltage slice. Pin coordinates and rotations are provenance-recorded; named nets and ground resolve through the shared `NetSolver`. Mirrors, unknown/custom symbols, non-static values, symbol semantic attributes, and non-`.op` directives remain explicit losses/unmapped entries rather than approximations. | Focused importer/reachability/registration suites pass 30/30; production build passes. Self-authored 6 V / 1 kOhm fixture reaches `Circuit.fromJSON` and solves to 6 mA; external paired-corpus qualification is a separate private lane. |

## DONE

| lane | owner | result | evidence |
|---|---|---|---|
| strict standard LTspice ASC Shockley diode import | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, after remotely merged claim `f95ee7f` and clarified shared-helper boundary `2e349b9`. The provenance-verified standard `diode` symbol maps SpiceOrder 1/2 to native anode/cathode. Only a self-contained, unambiguous local `D(IS,N,RS)` model with finite valid values and the existing fixed-thermal contract is numerically eligible. Missing/malformed/duplicate models, other model fields, mismatched or extra temperature options and instance attributes remain per-part blockers through Circuit JSON and export refusal. The existing SPICE model tokenizer is now one shared lexical helper; no model equation, library lookup or broad SPICE behavior changed. | Self-authored mirrored ASC and paired SPICE fixtures preserve named terminal partitions and exact model parameters through Circuit JSON and SPICE export/re-import. Node voltage, signed diode current and KCL agree with ngspice at 26.8267934421 C; reversing the verified pins fails the topology regression. Focused importer/SPICE/registration gate passes 54/54 and production build passes. Exact head `576c91b` CI run `34834373425` completed success before guarded fast-forward. Fixed-corpus reach is reported separately and is not inferred from parser success. |
| standard LTspice ASC inductor import | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, after the remotely merged claim `7d13924`. The bounded ASC importer maps the provenance-verified standard `ind` symbol to a native inductor only when Value resolves to a positive finite `henrys` scalar. The factual built-in A/B pin coordinates agree with a self-authored caller-supplied ASY pin contract. Zero/negative values and semantic attributes such as `Value2 Rser` remain persisted analysis blockers; no series-loss approximation or other symbol/model was added. | CUI board pin `7277e03` is a verified descendant of native ideal-L `9fe7ced`. A self-authored RL fixture preserves terminal partitions through ASC, Circuit JSON and SPICE export/re-import; ±5 mA DC currents and terminal KCL agree with ngspice, and a 10 us transient reaches the expected 4.97 mA magnitude. Swapping the built-in A/B coordinates fails the supplied-vs-built-in partition regression. Integrated ASC/export/source-analysis/registration gate passes 38/38 and production build passes with only pre-existing warnings. Fixed-corpus mapping and numerical receipts remain a separate private result, not inferred from parser success. |
| LTspice ASY document persistence and GUI bundle | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, after remotely merged claims `34769b9` and `0d84da9`. Imported bounded ASY definition metadata now survives Circuit JSON, GUI load/reset and undo/redo without becoming electrical parts. Every mapped ASC inherited or instance semantic-attribute loss also travels on its part, so parts/wires-only persistence cannot wash out numeric refusal. The existing GUI import picker accepts exactly one ASC plus caller-selected, unique-basename ASY files through an in-memory resolver, bounded to 256 files, 1 MiB each and 16 MiB total; it never resolves paths or network resources. | Self-authored tests retain an inherited `SpiceLine` blocker through parts/wires-only load and JSON round-trip and prove operating-point refusal; nested source metadata is independently cloned and undo/redo-safe. Import-menu reachability covers the one-ASC/count/byte/duplicate-name bounds, in-memory resolver and retained document handoff. Removing the per-part blocker fails the positive regression. Focused gate passes 57/57 and production build passes with pre-existing CSS/chunk warnings. No fake solver kind, implicit filesystem access, vendor payload, manual browser-click claim, or new electrical inference. |
| caller-supplied LTspice ASY reader | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14.** A pure bounded Version-4 reader accepts only caller-supplied symbol text or a synchronous caller resolver; it performs no path, network, include, or code execution. For existing exact standard electrical kinds, a valid CELL definition with unique contiguous SpiceOrder pins may replace built-in geometry and provide a default Value; instance attributes take precedence and an incompatible Prefix refuses mapping. Parsed custom symbols remain explicitly unmapped while import-result `sourceSymbols` retains their definition attrs, pin graph, drawing records, provenance declaration, and instance placements. No fake native kind, automatic resolver, vendor payload, exporter, or Circuit persistence was added. | Self-authored definitions prove source-record-order-independent SpiceOrder mapping, ASY default/instance override, current import connectivity, synchronous resolver caching, custom three-pin document retention without a native part, unsafe-name non-invocation, duplicate-order/prefix/size refusals, and caller-declared rather than verified hashes. Reversing SpiceOrder fails the positive fixture. Integrated ASC/source-analysis/reachability/registration gate passes 68/68; production build passes with only the pre-existing CSS/chunk warnings. GUI/CLI bundle selection and save/load persistence of document metadata remain explicit next slices. |
| KiCad SPICE voltage-source scalar value | `/root/zenodo_access_diagnosis` (Codex Sol) | **DONE 2026-09-14 in `0cae3c2`.** Existing `VSOURCE`/`VDC`/`VSRC` symbols retain only complete finite signed decimal/exponent or SPICE-SI static values as native volts. Missing values, waveform/functions, expressions, trailing units/junk and overflow remain unmapped with their raw Value reported; no default is manufactured. | Self-authored modern-KiCad fixtures prove `0`, `-2.5`, `1.2k` and `3m`, plus five refusal forms. Integrated KiCad/export-reachability/registration gate passes 99/99 and production build passes. Exact-head CI run `34824790604` completed success before guarded fast-forward. The independently measured ltspice2kicad fixture still has detached generated pin/wire geometry; this value fix does not move or connect anything. |
| native LTspice ASC exporter | `/root/zenodo_access_diagnosis` (Codex Sol) | **DONE 2026-09-14 in `f076561`.** The shared headless/UI export registry now writes a bounded Version-4 electrical-interchange ASC for standard resistor, capacitor, finite static voltage/current source and ground-label circuits. Deterministic local labels preserve supported electrical partitions without inventing routed wires. Unknown kinds, dynamic/extra parameters, semantic blockers and unrepresentable endpoints are reported; authored layout and ground-symbol identity are explicitly not claimed. | Self-authored R/C/V/I fixture round-trips 4/4 values and all three independently computed terminal partitions with zero importer losses/unmapped parts. Registry reachability, transfer reporting, test registration and integrated ASC/source-analysis gates pass 66/66; production build passes. Exact-head CI run `34824081720` completed success before guarded fast-forward. No vendor symbols, third-party converter code or corpus payload were committed. |
| LTspice ASC source-analysis contract | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, after remotely merged claim `93e6c1d`. ASC import now returns one source-name/terminal-membership record per electrical net and keeps true `.op/.ac/.tran/.dc` cards separate from retained parameter, model, and metadata directives. Only explicit `.backanno`/`.end` metadata is benign; unsupported model, temperature, function, and other directive semantics remain structured losses. Anonymous net names cannot case-fold onto any authored flag. No engine, generic adapter, exporter, automatic resolver, or external payload changed. | A self-authored paired ASC/SPICE fixture uses M90/M270/R90 orientations and signed current polarity, matches named net partitions independent of record order/label case, and reaches equal native OP topology, two node voltages, and source current. A reordered fixture proves structural comparison does not rely on source-order hashes. Collision mutation fails the authored `$asc$0` regression. Integrated ASC/PULSE/SINE/singleton/source-analysis/registration gate passes 48/48; production build passes. |
| source-declared analysis adapter | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, after remotely merged claim `62dc1b3`. A reusable public adapter enumerates every imported analysis card independently and wraps only existing `Circuit.operatingPoint()`, `BoardImpl.runAc()` and `Circuit.advanceTo()` APIs. OP runs immediately even beside other cards; strict bounded DEC AC and integer-nanosecond UIC transient grids run independently. Source-condition errors, importer-fidelity blockers, canonical-mapping gaps, solver refusals, unavailable APIs and unwired integration forms remain distinct outcomes. AC refuses authored current or multiple excitations rather than dropping them. No solver or importer physics changed. | Anonymous n0/s0 identities follow source topology order without exposing names; SPICE G polarity uses its native outn/outp mapping, and topology describes actual engine grounding so independent hashes expose alias drift. Repeated AC cards retain distinct IDs. Transient success checks the public engine convergence snapshot at every sample, and an RC regression proves each UIC run starts from a fresh uncharged state. Focused adapter/registration tests pass 15/15; integrated OP/AC/SPICE gate passes 90/90 and production build passes. |
| strict SPICE Shockley diode DC exposure | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-14**, implementation landed through `494930a` after remotely merged claim `6596fbb`. Exact explicit finite `D(IS,N,RS)` reaches the native fixed-thermal DC model; omitted temperature is disclosed as analysis-adapted, while exactly matched TEMP/TNOM is accepted. Malformed/duplicate model declarations, instance tails, unsupported diode physics and mismatched temperature remain structured blockers through Circuit JSON and are refused on re-export. Existing native LED/Vf-derived export is preserved. | Board pin `e175bf4` descends from diode `daf090c` and ideal-L `9fe7ced`. Self-authored signed diode/KCL and export/re-import tests agree with ngspice at the explicit 26.8267934421 C profile. Native junction regressions and 16/16 SPICE oracle decks pass. Exact candidate `26bdb71` CI run `34805709497` passed test, interaction, KiCad-oracle and SPICE-oracle jobs. |
| imported singleton electrical nets | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13.** SPICE and LTspice ASC imports annotate only source terminals whose named electrical node has one unique member. Circuit materializes each valid annotated terminal as an independent stable net only while no resolved wire, strip or externally supplied net owns it. The opt-in annotation survives JSON; wiring wins and disconnect restores it. No ordinary unwired pin, solver, source waveform, GUI/CLI or Lite behavior changed. | Self-authored unloaded voltage sources and a dangling resistor solve with their independent nodes retained; the resistor has only the engine's gmin leakage. Export consumes `resolvedNets` and recreates the singleton membership on SPICE re-import. Duplicate annotations, net-ID collision, invalid metadata, input-net immutability, JSON round trip, connect/disconnect, external-net sync and absence of self-loop wires are covered. Focused singleton tests pass 7/7; integrated SPICE/ASC/PULSE/registration gate passes 69/69 and production build passes atop PULSE master `6f8ebf7`. |
| exact seven-argument voltage PULSE import/export | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13**, after remotely merged claim `c196cfb`; implementation landed through `18c29bb`. ASC and SPICE voltage cards retain strict finite `PULSE(V1 V2 TD TR TF PW PER)` parameters through Circuit and SPICE export/re-import on board `041d82d`, a descendant of waveform engine `8049eff`. TR/TF/PER must be positive, TD/PW non-negative, and TR+PW+TF <= PER. Defaulted, zero-edge, extra, expression, overlap, current-wave and non-ideal variants remain explicit losses/refusals. | Focused plus integrated ASC/SINE/SPICE/EAGLE/registration gate passes 128/128 and production build passes. A self-authored imported 1 ns-edge RC compares 16 source/output observations to ngspice 42 with worst sampled output error 0.000397 V; asymmetric TR/TF mutation fails. Exact candidate `6d53e0c` CI run `34783587639` passed test, interaction, KiCad-oracle and SPICE-oracle checks. Fixed private-corpus coverage and numerical evidence are recorded separately. |
| LTspice ASC three-argument voltage SINE | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13 in `d4e2172`**, after remotely merged claims `39704e6` and `6e72ba3`. ASC and SPICE voltage cards retain strict finite three-argument `SINE(offset amplitude frequency)` through Circuit and lossless SPICE export/re-import. Defaulted/extra/non-finite/non-positive-frequency forms and current waves remain explicit losses. | Focused tests cover signed waveform/resistor-current samples against ngspice 42, refusal boundaries, export and re-import; exact-head CI run `34780541943` passed. The fixed 8,280-row private receipt records mapping and bounded generated-transient evidence separately. |
| controlled-source operating-point CLI/GUI/export exposure | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13 in `769fe7e`**, after remotely merged claim `360974c` and polarity amendment `3e68bc4`. CUI pins board `51c3c91`, exposes the engine's ideal E/G scope and supported kinds in `bwc op` and the existing Instruments panel, and exports only explicit finite ideal E/G cards. SPICE G first-to-second output current now maps to native `outn`→`outp`; E polarity is unchanged. Missing/non-finite gain or gm and any declared non-ideal rail/output/current-limit field are named export skips. | A self-authored imported E/G deck solves through the shipping CLI, preserves graph/parameters on export→import, and matches ngspice for signed nonzero node voltages, controlled-source currents, resistor current and KCL. Reverting the G mapping reverses the named regression. Integrated hierarchy/EAGLE/ASC/E/G/registration gate: 211/211 green; production build green. Exact-head CI `34777516098` green, including full tests, SPICE/KiCad oracles and the generic browser interaction gate. The controlled OP panel itself has source-level/unit reachability coverage, not a dedicated browser click smoke. No Lite package/deployment claim. |
| EAGLE unknown electrical-pin semantic-loss guard | `/root/zenodo_access_diagnosis` (Codex Sol) | **DONE 2026-09-13**, after remotely merged claim `a4980c7`. Unknown mapped-part pinrefs and pinrefs dropped because the engine model is narrower now retain source/net context as structured semantic losses. Existing warnings remain for display, while informational/geometry warnings are not promoted. No mapping or solver behavior changed. | Self-authored unknown-pin plus narrowed-model positives; shipped CLI and GUI analysis adapter both refuse before engine invocation. Focused suites pass 19/19. The fixed private 254-case rerun completes 254/254: 6,602 unknown + 26 narrowed-terminal losses across 220 files; all 37 formerly analysis-unblocked affected files now have blockers. Another 2,959 warnings remain warning-only. Structural denominators are unchanged; 0 simulations/numerical comparisons. |
| LTspice ASC capacitor/current-source extension | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13**, after remotely merged claim `dc196bd`. Added only provenance-backed standard `cap` and static `current` symbol rules. Capacitors retain order 1/2 as `a/b`; LTspice current order 1→2 maps to the established native `neg`→`pos` contract. Mirrors, dynamic/semantic attributes and unverified symbols remain loud losses or unmapped. | Rotated 2 mA source plus 1 kOhm load produces −2 V and signed KCL, independently matching ngspice 42 voltage/resistor current. A 10 µF parallel capacitor is zero-current in the native DC operating point while its divider remains 4 V/2 mA. Focused importer/reachability/registration suites pass 32/32 and production build passes. The fixed 8,280-row private census grows from 28 to 42 lossless mapped inputs; that is mapping reachability, not 42 numeric oracle passes. |
| KiCad one-level hierarchy import | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** Modern KiCad import accepts an explicit map of direct basename-only child sheets. Component/net identity is sheet-UUID scoped; local and hierarchical names remain instance-local, exact parent sheet pins bind child hierarchical labels, and only global/power names cross all sheets. Missing, unsafe, ambiguous, deep and cyclic inputs are structured semantic losses that block numeric analysis while retaining the safe partial display. The existing API, `bwc` CLI and multi-file GUI picker share the same resolver; it performs no file/network reads. | Self-authored repeated-child parent/child fixtures preserve the three-net partition through `Circuit` save/load and use separate display offsets. Removing instance scope makes the positive topology regression fail. Focused importer/registry/OP/report suites pass 149/149 and production build passes. The external recovered Water Sump Pump root now imports 86 mapped parts, 102 wires and 35 nets from two children, with 7 components explicitly unmapped and 0 hierarchy losses. Import/connectivity evidence only: 0 simulations and 0 numeric oracle comparisons. |
| EAGLE exponent-form passive value round-trip | `/root/zenodo_access_diagnosis` (Codex Sol) | **DONE 2026-09-13**, after remotely merged claim `82681d3`. `parseEagleValue` accepts a complete decimal exponent grammar emitted by the existing exporter while incomplete exponent strings remain invalid. No exporter, pin mapping, or solver behavior changed. | Self-authored `1e-9` and uppercase `2.2E-6` positives plus malformed `1e` negative; focused EAGLE import/round-trip suites pass 26/26 with the real bw-board sibling. The fixed private 254-case corpus rerun is recorded separately. |
| static operating-point GUI visibility | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** The existing Instruments simulation controls now run the engine-owned one-shot DC operating point and display convergence, supported scope, C/source semantics, node voltages and signed terminal currents. Import `unmapped[]`/`losses[]` become persisted analysis blockers; a blocked import shows a refusal without calling the engine. No live transient state is adopted. | Actual engine RC action returns 6 V / 2 V, 2 mA and C-open while the before/after board snapshot stays equal. Self-authored `wavefile=` loss preserves its source card through save/load and refuses pre-engine; unmapped LM193-style input is also blocked. Focused suites pass 39/39 and production build passes. |
| static operating-point CLI reachability | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** `Circuit.operatingPoint()` is a thin engine proxy and `bwc op` imports a circuit, refuses any unmapped or semantic-loss input, and prints the engine's independent static-DC voltages and signed terminal currents without adopting them into transient state. The pinned engine is the landed API SHA. | Self-authored RC deck solves to 6 V / 2 V and 2 mA with the capacitor open; external `wavefile=` is refused before simulation. Focused model/import/registration suites pass 86/86 and production build passes. |
| SPICE external-waveform semantic loss | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** Independent V/I cards using `wavefile=` remain partially importable at the existing fixed fallback, but now emit a loud warning and a structured `losses[]` entry retaining the full logical source card, reason, and fallback. `bwc info` displays the loss, and both numeric-oracle re-import paths refuse lossy imports before simulation. No waveform file is read or executed; numeric DC/SINE/PULSE paths remain loss-free. | Self-authored nonexistent-file positive and three supported-form negatives; focused importer/corpus/reachability suites pass 62/62 and the existing SPICE oracle passes 16/16 supported decks. The actual local `wavein.net` remains 2 parts/1 wire/0 unmapped but is now explicitly 1 semantic loss and therefore not simulation/oracle evidence. |
| single-element SPICE dispatch | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** A one-element file is recognized as SPICE only when it also has both a recognized analysis/model card and an exact `.end`; one-card fragments missing either marker remain refused, and structured KiCad `(export ...)` still wins before SPICE. | Self-authored `.net` fixture, two near-miss negatives, genuine KiCad negative, and shipping `bwc info`; focused suites 53/53 pass. Symbench dispatch is now 4,342/4,342 `.net` files to SPICE, with the actual `wavein.net` importing 2 parts/1 wire/0 unmapped; 145 incomplete `.cir2/.cir3` files remain unrecognized. Dispatch/import evidence only: waveform semantics are not modeled and 0 simulations/0 numeric comparisons were run. |
| controlled-source slug-coverage CI repair | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13.** The reasoned coverage exceptions now record `vcvs`/`vccs` as importer-only engine-native primitives with explicit terminal contracts and deliberately no UI sidecar/palette entry. No production behavior changed. | Exact prior-head CI failure was confined to two stale-slug assertions; focused slug coverage and SPICE integration suites pass after the test-only correction. |
| SPICE independent-current polarity contract | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13.** SPICE positive I-card flow (first node to second) now maps symmetrically to bw-board positive `isource` flow (`neg` to `pos`) on import and export. Source magnitude stays positive; V-source semantics and engine code are unchanged. | Both card orientations solve through `Circuit.fromJSON` to the analytical -2 V/+2 V results for 2 mA through 1 kOhm, satisfy KCL, and preserve topology/direction on export/import. Focused SPICE suites: 52/52 pass; production build passes. |
| KiCad legacy verified rescued resistors | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** `R-RESCUE-<project>` maps to a resistor only when its exact supplied library definition is exactly passive pins 1/2; the original libsource is retained. An extra-pin definition is refused, and no generic suffix or comparator mapping was added. | Self-authored positive/ambiguous-negative fixture and shipping CLI path; focused merged-head suites 107/107 pass. Both local PySpice legacy schematics now map 12/13 placements (three rescued resistors each), retain all 14/14 KiCad-exported SPICE topology partitions, and leave only LM193 explicitly unmapped. Import evidence only: 0 simulations and 0 numeric oracle comparisons. |
| KiCad legacy hidden-definition lookup | `/root/schematic_corpus_import` (Codex Sol) | **DONE 2026-09-13.** Cache-library lookup now accepts the format's exact leading-`~` hidden-symbol spelling only after exact-name lookup, restoring rescued power-symbol pin geometry without fuzzy symbol matching. | Self-authored connected-rail and near-name negative fixtures plus shipping `bwc info`; focused suites 74/74 pass. Both local PySpice KiCad legacy schematics match their independent KiCad-exported SPICE topology: 2/2 files, 14/14 multi-node partitions. This is import topology evidence only: 0 simulations, and unsupported comparator/rescued resistor mappings remain reported. |
| controlled-source terminal contract | `/root/sol_lane_coordination` (Codex Sol) | **DONE 2026-09-13.** Core `vcvs`/`vccs` parts now retain bw-board's `outp/outn/inp/inn` contract through SPICE import and `Circuit.fromJSON`; stale `a/b` input remains a loud rejection. No solver or importer-card mapping changed. | Focused Node suite: 105/105 pass. The reviewed private PhyChip case now reaches all three finite comparisons on clean bw-board `838492f`; two agree and the resistor-current observable exposes a separate sign-orientation manifest correction. |
